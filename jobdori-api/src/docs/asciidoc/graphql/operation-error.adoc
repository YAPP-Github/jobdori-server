// 이 파일은 ErrorCodeDocsGeneratorTest에서 생성됩니다. 직접 수정하지 마세요.

// tag::credit[]
커스텀 에러 코드: 없음

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요.
// end::credit[]

// tag::me[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| user_not_found
| 가입된 계정을 찾지 못했어요. 회원가입 후 이용해 주세요.
| 유저를 찾을 수 없는 경우

|===
// end::me[]

// tag::experience[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_not_found
| 경험을 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험을 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| `project` 필드를 함께 요청했으나 연결된 경험 프로젝트를 찾을 수 없는 경우

|===
// end::experience[]

// tag::experiences[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| `projectId`로 특정 프로젝트의 경험 목록을 조회했으나 프로젝트를 찾을 수 없는 경우

|===
// end::experiences[]

// tag::searchExperiences[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::searchExperiences[]

// tag::experienceProject[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험 프로젝트를 찾을 수 없는 경우

|===
// end::experienceProject[]

// tag::experienceProjects[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::experienceProjects[]

// tag::createExperience[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험 프로젝트를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| 경험 내용을 FREE로 입력해 AI 추출/재작성을 요청했으나 잔여 크레딧이 0인 경우

|===
// end::createExperience[]

// tag::updateExperience[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_not_found
| 경험을 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험을 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험 프로젝트를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| 경험 내용을 FREE로 입력해 AI 추출/재작성을 요청했으나 잔여 크레딧이 0인 경우

|===
// end::updateExperience[]

// tag::deleteExperience[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_not_found
| 경험을 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험을 찾을 수 없는 경우

|===
// end::deleteExperience[]

// tag::createExperienceProject[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::createExperienceProject[]

// tag::updateExperienceProject[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험 프로젝트를 찾을 수 없는 경우

|===
// end::updateExperienceProject[]

// tag::deleteExperienceProject[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| experience_project_not_found
| 프로젝트를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 경험 프로젝트를 찾을 수 없는 경우

|===
// end::deleteExperienceProject[]

// tag::connectNotion[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| notion_api_request_failed
| Notion에서 내용을 가져오지 못했어요. 잠시 후 다시 시도해 주세요.
| Notion OAuth 토큰 교환 요청이 실패한 경우

|===
// end::connectNotion[]

// tag::disconnectNotion[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::disconnectNotion[]

// tag::notionConnections[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::notionConnections[]

// tag::notionPages[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| notion_connection_not_found
| Notion 연결에 실패했어요. Notion을 다시 연결해 주세요.
| 요청한 Notion 연결이 없거나 접근 권한이 없는 경우

| notion_connection_need_reconnect
| Notion 인증이 만료되었어요. Notion을 다시 연결해 주세요.
| Notion 토큰 갱신에 실패했거나 연결 권한이 해제된 경우

| notion_page_access_denied
| Notion 페이지에 접근할 수 없어요. Notion에서 페이지 공유 설정을 확인해 주세요.
| 해당 페이지가 연결에 공유되지 않았거나 권한이 없는 경우

| notion_api_request_failed
| Notion에서 내용을 가져오지 못했어요. 잠시 후 다시 시도해 주세요.
| Notion API 호출이 실패한 경우

|===
// end::notionPages[]

// tag::importNotionExperiences[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| notion_connection_not_found
| Notion 연결에 실패했어요. Notion을 다시 연결해 주세요.
| 요청한 Notion 연결이 없거나 접근 권한이 없는 경우

| notion_connection_need_reconnect
| Notion 인증이 만료되었어요. Notion을 다시 연결해 주세요.
| Notion 토큰 갱신에 실패했거나 연결 권한이 해제된 경우

| notion_page_access_denied
| Notion 페이지에 접근할 수 없어요. Notion에서 페이지 공유 설정을 확인해 주세요.
| 해당 페이지가 연결에 공유되지 않았거나 권한이 없는 경우

| notion_api_request_failed
| Notion에서 내용을 가져오지 못했어요. 잠시 후 다시 시도해 주세요.
| Notion API 호출이 실패한 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| 잔여 크레딧이 0인 상태에서 AI 기능을 호출한 경우

|===
// end::importNotionExperiences[]

// tag::resumes[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::resumes[]

// tag::resumeCounts[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::resumeCounts[]

// tag::resume[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| resume_not_found
| 이력서를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 이력서를 찾을 수 없는 경우

|===
// end::resume[]

// tag::createResume[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| `optimizationMode`가 `JOB_SPECIFIC`이라 경험 AI 첨삭을 수행하는데 잔여 크레딧이 0인 경우

|===
// end::createResume[]

// tag::updateResume[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| `optimizationMode`가 `JOB_SPECIFIC`이라 경험 AI 첨삭을 수행하는데 잔여 크레딧이 0인 경우

| resume_not_found
| 이력서를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 이력서를 찾을 수 없는 경우

|===
// end::updateResume[]

// tag::deleteResume[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| resume_not_found
| 이력서를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 이력서를 찾을 수 없는 경우

|===
// end::deleteResume[]

// tag::jd[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| jd_not_found
| 등록되지 않은 채용공고예요. 목록에서 다시 확인해 주세요.
| 존재하지 않거나 소유자가 아닌 JD 조회

|===
// end::jd[]

// tag::jds[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::jds[]

// tag::registerJd[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| 잔여 크레딧이 0인 상태에서 AI 기능을 호출한 경우

| experience_required
| 이력서 생성을 위한 경험이 필요해요. 내 경험 탭에서 경험을 추가해 주세요.
| 워크스페이스에 활성 상태의 경험이 없어 JD를 등록할 수 없는 경우

| jd_invalid_url
| 링크 형식이 올바르지 않아요. 확인 후 다시 시도해 주세요.
| 허용되지 않은 스킴이거나 내부 네트워크 주소(SSRF 차단)

| jd_access_denied
| 이 페이지에 접근할 수 없어요. 원문을 직접 복사해 붙여 넣어 주세요.
| JD URL 접근 거부(4xx)

| jd_fetch_failed
| 내용을 불러올 수 없어요. 원문을 직접 복사해 붙여 넣어 주세요.
| JD 본문 수집 실패(모든 크롤 단 실패)

| jd_not_a_posting
| 채용공고가 아닌 것 같아요. 채용공고 주소나 내용을 확인해 주세요.
| AI가 채용 공고로 인식하지 못한 본문/URL

| ai_rate_limited
| 요청이 많아 잠시 기다려야 해요. 입력한 내용은 그대로 있으니 잠시 후 다시 시도해 주세요.
| AI 요청 한도 초과

| ai_generation_failed
| 생성이 중간에 멈췄어요. 입력한 내용은 그대로 있으니 다시 시도해 주세요.
| AI 응답 생성 실패

| ai_unavailable
| 지금은 AI 기능을 이용할 수 없어요. 잠시 후 다시 시도해 주세요.
| AI 서비스 접근 불가

| ai_timeout
| 응답이 늦어지고 있어요. 잠시 후 다시 시도해 주세요.
| AI 응답 지연/시간 초과

|===
// end::registerJd[]

// tag::markJdCompleted[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| jd_not_found
| 등록되지 않은 채용공고예요. 목록에서 다시 확인해 주세요.
| 존재하지 않거나 소유자가 아닌 JD 조회

|===
// end::markJdCompleted[]

// tag::profile[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::profile[]

// tag::updateProfile[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

|===
// end::updateProfile[]

// tag::generateCoreCompetency[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| invalid_arguments
| 입력한 내용을 다시 확인해 주세요.
| 필수 파라미터가 없거나, 파라미터가 유효하지 않는 경우

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| resume_not_found
| 이력서를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 이력서를 찾을 수 없는 경우

| jd_not_found
| 등록되지 않은 채용공고예요. 목록에서 다시 확인해 주세요.
| 존재하지 않거나 소유자가 아닌 JD 조회

| ai_generation_failed
| 생성이 중간에 멈췄어요. 입력한 내용은 그대로 있으니 다시 시도해 주세요.
| AI 응답 생성 실패

|===
// end::generateCoreCompetency[]

// tag::polishProfileText[]
커스텀 에러 코드:

전역 공통 에러 코드는 <<graphql-common-error-codes,공통 에러 코드>>를 참고해 주세요. +
아래에는 각 GraphQL operation의 요청 샘플과 도메인 로직에 따라 발생할 수 있는 커스텀 에러 코드만 명시합니다.

[.api-table]
[cols="28%,32%,40%",options="header"]
|===
| Code | Message | Description

| invalid_arguments
| 입력한 내용을 다시 확인해 주세요.
| 필수 파라미터가 없거나, 파라미터가 유효하지 않는 경우

| workspace_access_denied
| 이 워크스페이스에 접근할 권한이 없어요.
| 워크스페이스 접근 권한이 없는 경우

| workspace_not_found
| 워크스페이스를 찾지 못했어요. 목록에서 다시 확인해 주세요.
| 워크스페이스를 찾을 수 없는 경우

| insufficient_credit
| 오늘의 크레딧을 모두 소진했습니다.
| 잔여 크레딧이 0인 상태에서 AI 기능을 호출한 경우

| jd_not_found
| 등록되지 않은 채용공고예요. 목록에서 다시 확인해 주세요.
| 존재하지 않거나 소유자가 아닌 JD 조회

| ai_generation_failed
| 생성이 중간에 멈췄어요. 입력한 내용은 그대로 있으니 다시 시도해 주세요.
| AI 응답 생성 실패

|===
// end::polishProfileText[]


